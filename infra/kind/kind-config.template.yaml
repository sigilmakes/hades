# Template used by scripts/dev-setup.sh for local kind development.
# The script substitutes __HADES_KIND_DATA_DIR__ with an absolute host path
# outside the repo, then creates the cluster with the generated config.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraMounts:
      - hostPath: __HADES_KIND_DATA_DIR__
        containerPath: /data/hades
        readOnly: false
        propagation: Bidirectional
